# Secular Plot

This directory contains the script used to generate the secular-function figure
for Chapter 2.

Run:

```bash
/home/ec2-user/MATH5005-5006_mingyin/.venv/bin/python plot_secular.py
```

It writes the generated files to `output/` and copies the PNG/PDF used by LaTeX
to `latex/figures/chapter2/`.
