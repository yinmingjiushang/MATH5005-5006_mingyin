# Section 4.6.3 - Hardware-Efficiency Evidence

This is the canonical entry point for Chapter 4 Section 4.6.3.

- thesis section: `4.6.3 Hardware-Efficiency Evidence`
- implementation layout: local `src/`, `script/`, `output/`
- output location: `Code/chapter4/04_06_03_hardware_efficiency/output`

Use:

- `build_all.sh` to build the binaries
- `run_perf.sh` to collect `perf stat` counters
- `run_mem.sh` to collect peak-memory measurements
